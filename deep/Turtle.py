import turtle
import turtle # Allows us to use turtles
window = turtle.Screen() # Creates a playground for turtles
bbb = turtle.Turtle() # Create a turtle, assign to alex

bbb.forward(50) # Tell alex to move forward by 50 units
bbb.left(90) # Tell alex to turn by 90 degrees
bbb.forward(30) # Complete the second side of a rectangle8
window.mainloop() # Wait for user to close window